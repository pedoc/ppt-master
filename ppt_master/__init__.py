"""Lightweight package shim for pip/git installation.

This package makes the repository installable via `pip install git+https://...`.
It intentionally contains minimal runtime code; the full tools live under `skills/ppt-master`.
"""
__version__ = "2.3.0"
